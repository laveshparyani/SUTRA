# Central tier dependencies — command centre only.
# Deliberately excludes opencv, onnxruntime and the ANPR model packages: the
# central node never decodes video or runs inference, so it stays small enough
# for a free 512 MB instance.
fastapi
uvicorn[standard]
sqlalchemy
psycopg[binary]
pydantic
pydantic-settings
httpx
numpy
# headless build: no GUI/GL dependencies. Imported at module scope by the
# shared services; the central tier never actually decodes a frame.
opencv-python-headless
python-multipart
websockets
pyjwt
