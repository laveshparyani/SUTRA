# large sample footage — never commit
CCTV Control Room/
*.mp4
*.mkv
*.avi

# runtime data dir at the repo root only (db, frames, evidence, secrets).
# NOT a bare `data/`, which also matched backend/app/connectors/data and kept
# the packaged representative datasets out of the deployed image.
/data/
*.jpg
*.png

# python
.venv/
venv/
__pycache__/
*.pyc

# node
node_modules/
dist/

# env & secrets
.env
*.local
infra/mediamtx/

# runtime secret — never commit
data/.jwt_secret

# tunnel client binary and logs (downloaded locally, not source)
infra/tunnel/

# root node_modules from the presentation build tool (pptxgenjs, --no-save)
/node_modules/
